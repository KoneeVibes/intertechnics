import { styled } from "@mui/material/styles";
import { Container } from "@mui/material";

export const AssetIntegrityManagementWrapper = styled(Container)(() => {
	return {
		overflow: "hidden",
	};
});
